# SUPABASE ACCESS MATRIX

## Tabelas Mapeadas
- `integrity_channels`: Exclusivamente Server-side (leitura parcial via endpoint).
- `integrity_reports`: Server-side / API
- Geral: RLS habilitado mas focado no proxy via backend Express.
